services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: gtmcopilot
      POSTGRES_PASSWORD: gtmcopilot
      POSTGRES_DB: gtmcopilot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
